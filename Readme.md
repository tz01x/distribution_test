# share object with ray in distributed process
In the Ray framework, you can share objects between multiple nodes using the concept of Ray's distributed memory store. This is managed by the Ray runtime and enables efficient sharing of data across nodes without explicitly copying it
